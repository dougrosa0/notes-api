{
  "name": "user-notes-api",
  "version": "1.0.0",
  "description": "API for user notes and day rankings",
  "main": "src/api-server.js",
  "scripts": {
    "start": "node src/api-server.js",
    "dev": "nodemon src/api-server.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.0.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}